Run full project validation: typecheck, lint, and tests.

1. Run typecheck: `bun run check`
2. Run linter: `bun run lint`
3. Run tests: `bun run --cwd server test`

Report any failures with file paths and line numbers.
